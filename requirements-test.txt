# Minimal dependencies for running the test suite.
# The notebook itself needs TensorFlow + xgboost + pymc, listed in a separate
# requirements.txt that will be added in Phase 3.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pytest>=7.0
